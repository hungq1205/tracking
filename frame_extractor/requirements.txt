# Installed on top of scan_server/requirements.txt in the same `hrtf` conda env.
#
# transformers is pinned (not left on whatever scan_server/hrtf otherwise
# has) because the `ram` package below vendors its own BERT copy against an
# older transformers internal API — see CLAUDE.md's "Frame Extractor"
# section for what specifically breaks above this version.
#
# After installing, run `python frame_extractor/patch_ram_package.py` once —
# `ram`'s vendored BERT still has a few import-path breakages even at this
# pinned transformers version; that script patches the installed package
# in place (idempotent, safe to re-run).
transformers==4.46.3
accelerate>=0.26.0
timm
fairscale
git+https://github.com/xinyu1205/recognize-anything.git
