# Installed on top of scan_server/requirements.txt in the same `hrtf` conda env.
#
# transformers is pinned (not left on whatever scan_server/hrtf otherwise
# has) because AutoModelForZeroShotObjectDetection's
# post_process_grounded_object_detection has a different signature above
# this version (box_threshold/"labels" vs. threshold/"text_labels") — see
# CLAUDE.md's "Frame Extractor" section.
#
# google-genai (Gemini API client, used by tagging.py's FrameTagger for the
# open-set tagging step) is already pinned in scan_server/requirements.txt —
# no separate entry needed here.
#
# RAM++ (`recognize-anything` package + patch_ram_package.py) is no longer
# used by the live tagging pipeline — tagging.py switched to Gemini for
# open-set tagging (see that module's docstring); both are kept in the repo
# for reference/future use, not deleted.
transformers==4.46.3
accelerate>=0.26.0
timm
fairscale
