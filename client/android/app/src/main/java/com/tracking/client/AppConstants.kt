package com.tracking.client

// No hardcoded frame resolution constants — the app uses the camera's native output resolution.
// Frame dimensions are read at runtime from the actual JPEG header (see MainViewModel).
